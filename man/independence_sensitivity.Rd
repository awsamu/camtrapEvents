% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/independence_sensitivity.R
\name{independence_sensitivity}
\alias{independence_sensitivity}
\title{Sensitivity of event totals to threshold and rule}
\usage{
independence_sensitivity(
  data,
  datetime,
  station,
  species = NULL,
  thresholds = c(0, 15, 30, 60, 120),
  rules = c("time_only", "running_max", "any_change"),
  metadata = NULL,
  count = NULL,
  compare_to = c("last_record", "last_independent"),
  format = "\%Y-\%m-\%d \%H:\%M:\%S",
  tz = "UTC",
  by_species = TRUE
)
}
\arguments{
\item{data}{A data frame of camera-trap records, one row per detection.}

\item{datetime}{Name of the date-time column. Either \code{POSIXct}, or
character parsed with \code{format}.}

\item{station}{Name of the column identifying the camera or station.
Independence is assessed within station.}

\item{species}{Name of the species column. Independence is assessed within
species. Pass \code{NULL} to pool all species.}

\item{thresholds}{Numeric vector of thresholds in minutes.}

\item{rules}{Character vector of rules to compare.}

\item{metadata}{Character vector of column names carrying the metadata used
by \code{rule}. Ignored when \code{rule = "time_only"}.}

\item{count}{Optional name of a total group-size column, used by
\code{"running_max"} in addition to \code{metadata}, and used to compute
\code{n_new}. If absent, group size falls back to the sum of numeric
\code{metadata}.}

\item{compare_to}{Reference point for the time gap:
\describe{
  \item{\code{"last_record"}}{Gap measured from the previous record, retained
    or not. A burst ends only after \code{threshold} elapses with no records
    at all. Default, and the behaviour of most published filters.}
  \item{\code{"last_independent"}}{Gap measured from the last retained event,
    subdividing long bursts at fixed intervals.}
}
Equivalent to \pkg{camtrapR}'s \code{deltaTimeComparedTo}.}

\item{format}{Format string used to parse \code{datetime} when it is character.}

\item{tz}{Time zone used for parsing. Defaults to \code{"UTC"}.}

\item{by_species}{If \code{TRUE}, also return counts per species.}
}
\value{
A list with:
  \describe{
    \item{\code{overall}}{data frame: rule, threshold, records, events, pct_retained}
    \item{\code{by_species}}{data frame of per-species counts, or \code{NULL}}
    \item{\code{inflation}}{per-species percentage increase of each rule over
      \code{"time_only"} at the same threshold, or \code{NULL} if
      \code{"time_only"} was not among \code{rules}}
  }
}
\description{
Runs \code{\link{independent_events}} across a grid of thresholds and rules
and returns event counts, overall and optionally per species. The choice of
independence filter is rarely justified from data, so reporting this grid
turns an arbitrary decision into a stated sensitivity analysis.
}
\details{
The per-species output is the more diagnostic of the two. If the extra events
produced by a metadata rule are concentrated in gregarious species, the rule
is introducing a bias that correlates with sociality, which will propagate
into any comparative index such as RAI.
}
\examples{
set.seed(1)
n <- 200
recs <- data.frame(
  station  = sample(c("CAM01", "CAM02"), n, TRUE),
  species  = sample(c("solitary_sp", "herd_sp"), n, TRUE),
  datetime = as.POSIXct("2021-01-01", tz = "UTC") +
               cumsum(sample(c(60, 300, 5000), n, TRUE)),
  adults   = sample(1:6, n, TRUE)
)
s <- independence_sensitivity(
  recs, "datetime", "station", "species",
  thresholds = c(15, 30, 60), metadata = "adults"
)
s$overall

}
\seealso{
\code{\link{independent_events}}
}
